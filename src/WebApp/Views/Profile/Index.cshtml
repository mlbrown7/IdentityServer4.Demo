
@{
}

<div class="row">
    <div class="col-md-3">
        <h2>Profile</h2>
    </div>

    <u>Claims</u>
    <dl>
        @foreach (var claim in User.Claims)
        {
            <dt>@claim.Type</dt>
            <dd>@claim.Value</dd>
        }
    </dl>
</div>